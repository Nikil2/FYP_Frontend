"use client";

/**
 * WhatsApp + Phone floating buttons were removed in favour of the Nova AI
 * assistant (see components/ai/AIChatWidget). Kept as a no-op so existing
 * imports across customer pages don't break.
 */
export function FloatingButtons() {
  return null;
}
